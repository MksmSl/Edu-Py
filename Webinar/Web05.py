# Create a module `lang.py` that has a function that takes a string as input and uses library to detect the language. In a `main.py`, import this function, ask user for the filename, read the file, and print its language.

# Steps:

# 1. Create a virtual env

#     ```bash
#     > pip install virtualenv
#     >
#     > python3 -m venv venv_name
#     >
#     > source venv_name/bin/activate
#     >
#     >
#     > pip --version
#     ```

# 2. Search for library
# 3. Install the found library
# 4. Write `lang.py`
# 5. Write `main.py`